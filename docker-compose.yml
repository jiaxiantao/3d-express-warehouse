services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3100:3100"
    environment:
      NODE_ENV: production
      PORT: "3100"
      NEXT_PUBLIC_SITE_URL: http://localhost:3100
